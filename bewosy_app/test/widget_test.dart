import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Smoke test placeholder — full integration tests go in integration_test/
    expect(true, isTrue);
  });
}
